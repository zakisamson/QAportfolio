Feature: End to End

    Scenario: Successfully checkout a single item
        Given the user is logged in as a standard user
        When the user clicks on the "Sauce Labs Backpack" item name
        And the user clicks the "Add to cart" button on the item detail page
        And the user clicks the Cart icon on the navigation bar
        And the user clicks the "Checkout" button on the Your Cart page
        And the user fills in the checkout information with valid data
        And the user clicks the "Continue" button on the Checkout Information page
        And the user clicks the "Finish" button on the Checkout Overview page
        Then the order is placed successfully

    Scenario: Successfully checkout multiple items
        Given the user is logged in as a standard user
        When the user clicks on the "Sauce Labs Backpack" item name
        And the user clicks the "Add to cart" button on the item detail page
        And the user clicks the "Back to products" button
        And the user clicks on the "Sauce Labs Bike Light" item name
        And the user clicks the "Add to cart" button on the item detail page
        And the user clicks the Cart icon on the navigation bar
        And the user clicks the "Checkout" button on the Your Cart page
        And the user fills in the checkout information with valid data
        And the user clicks the "Continue" button on the Checkout Information page
        And the user clicks the "Finish" button on the Checkout Overview page
        Then the order is placed successfully